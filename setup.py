from setuptools import setup, find_packages

setup(
    name='hawkpass',
    version='0.0.1',
    author='Petros Kalogiannakis',
    author_email='dev@petroskal.com',
    description='A simple cryptographically secure (SHA-256) diceware password generator that supports multiple languages.',
    url='https://github.com/kalpetros/hawkpass_py',
    install_requires=[],
    py_modules=['hawkpass']
)
